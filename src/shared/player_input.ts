/* Simple struct to track state of input (pressed or not) */
// TODO: share with server
export class PlayerInput {
    up: boolean;
    down: boolean;
    left: boolean;
    right: boolean;
    shoot: boolean;
}
